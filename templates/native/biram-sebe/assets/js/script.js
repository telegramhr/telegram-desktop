window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-2');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-2');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});



window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-3');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-3');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-4');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-4');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-5');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-5');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});



window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-6');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-6');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});



window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-7');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-7');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-8');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-8');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-9');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + scrollPosition / 1 + 'px)'; 
});


window.addEventListener('scroll', function() {
  var scrollTransform = document.querySelector('.scroll-transform-kontra-9');
  var scrollPosition = window.scrollY;

  // Primena transformacije na osnovu vrednosti skrolovanja
  scrollTransform.style.transform = 'translateX(' + (-scrollPosition / 1) + 'px)';
});


document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});


document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-2');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});

document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-3');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});

document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-4');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});



document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-5');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});


document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-6');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});


document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-7');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});

document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-8');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});


document.addEventListener('DOMContentLoaded', function () {
  const marqueeContent = document.querySelector('.marquee-content-9');
  const originalText = marqueeContent.textContent;
  let newText = '';

  for (let i = 0; i < 50; i++) { // U ovom slučaju 50 je proizvoljan broj ponavljanja teksta
    newText += originalText + ' ';
  }

  marqueeContent.textContent = newText;
});

